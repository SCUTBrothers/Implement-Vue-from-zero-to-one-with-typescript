import InsertText from "./text"
import If from "./if"
import On from "./on"
import For from "./for"
import Bind from "./bind"

export default {
    text: InsertText,
    if: If,
    on: On,
    for: For,
    bind: Bind
}